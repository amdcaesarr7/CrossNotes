# CrossNotes — Firebase Configuration
# Copy this to .env and fill in your Firebase project values.
# Get these from: Firebase Console → Project Settings → General → Your apps → Web app

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# IMPORTANT — Firebase Console setup checklist:
# 1. Enable Authentication → Google sign-in provider
# 2. Add your domain to Authorized domains (Authentication → Settings → Authorized domains)
#    - Add: your-replit-dev-domain.replit.dev (get it from REPLIT_DEV_DOMAIN env var)
# 3. Create Firestore Database (Firestore Database → Create database)
# 4. Set Firestore rules (allow read/write for authenticated users + public leaderboard read):
#
# rules_version = '2';
# service cloud.firestore {
#   match /databases/{database}/documents {
#     match /subjects/{subject} {
#       allow read: if true;
#       match /chapters/{chapter} {
#         allow read: if true;
#         match /notes/{note} { allow read: if true; }
#         match /flashcards/{card} { allow read: if true; }
#         match /quiz/{q} { allow read: if true; }
#       }
#     }
#     match /users/{uid} {
#       allow read, write: if request.auth != null && request.auth.uid == uid;
#       match /progress/{chapter} {
#         allow read, write: if request.auth != null && request.auth.uid == uid;
#       }
#     }
#     match /leaderboard/{uid} {
#       allow read: if true;
#       allow write: if request.auth != null && request.auth.uid == uid;
#     }
#   }
# }

# Server-only Vercel Function configuration. Never prefix these with VITE_ and never expose them in frontend code.
# No-cost email delivery through the support.crossnotes@gmail.com Apps Script relay.
GMAIL_RELAY_URL=
GMAIL_RELAY_SECRET=
NOTIFICATION_EMAIL_TO=caesar.anwarr91@gmail.com

# Explicit production-origin allow-list (for example, https://cross-notes-crossnotes-i2u7.vercel.app).
ALERT_ALLOWED_ORIGIN=
